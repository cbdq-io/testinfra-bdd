---
version: "3"

services:
  sut:
    build: sut
    command:
      - "sleep"
      - "infinity"
    container_name: sut

  java11:
    command:
      - "sleep"
      - "infinity"
    container_name: java11
    image: "amazoncorretto:11"
